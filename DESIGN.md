# DESIGN

## 무엇인가

박유진(고려대학교 인공지능학과)의 자기소개 페이지입니다.
스크롤 없이 한 화면에서 끝나는 한 장짜리 소개로, 이름·소속·취미를
**수능 당일 시험 시간표** 형식에 얹어 보여줍니다.
아직 없는 것(프로젝트, 관심 분야, 연락처)은 감추지 않고 빈 칸으로 남겨
나중에 채울 자리로 드러냅니다.

## 화면에 있는 것

전체가 책상색 배경 위에 시험지 한 장이 놓인 모양입니다.
그 안에 위에서부터 네 덩어리가 들어갑니다.

### 1. 머리말

- 작은 글씨로 `2026학년도`
- 큰 제목 **자기소개 시간표** — 명조체, 가운데 정렬
- 아래에 굵은 선 + 가는 선 이중 밑줄 (공문서 제목 형식)

### 2. 수험생 정보란

테두리로 둘러싼 네 칸이 가로로 나란히 놓입니다. 각 칸은 작은 라벨 + 값입니다.

| 칸 | 값 |
|---|---|
| 성명 | 박유진 |
| 소속 | 고려대학교 인공지능학과 |
| 수험번호 | *비어 있음 — 짧은 밑줄만* |
| 확인 | 빨간 테두리의 `미정` 도장 |

### 3. 시험 시간표 (표)

`교시 · 영역 · 시간 · 소요` 네 개 열의 표입니다.
머리글 줄은 위아래로 선이 있고, 마지막 줄 아래에 굵은 마감선이 들어갑니다.

| 교시 | 영역 | 시간 | 소요 |
|---|---|---|---|
| — | 입실 완료 | ~ 08:10 | — |
| 1교시 | 인공지능 (부제: 고려대학교 인공지능학과) | 08:40 ~ 10:00 | 80분 |
| 2교시 | 기타 🎸 (부제: E A D G B E) | 10:30 ~ 12:00 | 100분 |
| — | 점심시간 | 12:00 ~ 13:00 | 60분 |
| 3교시 | 관심 분야 — *비어 있음* | 13:10 ~ 14:20 | 70분 |
| 4교시 | 프로젝트 — "아직 없어요…" | 14:50 ~ 16:37 | 107분 |
| 5교시 | 연락처 — *비어 있음* | 17:05 ~ 17:45 | 40분 |

세부 규칙:

- **시간은 실제 대학수학능력시험 시간표**를 그대로 씁니다.
- 교시가 아닌 줄(입실 완료, 점심시간)은 **음영 배경 + 흐린 글자**로 눌러
  한눈에 구분됩니다.
- 영역 칸은 굵은 제목 한 줄 + 그 아래 작은 부제 한 줄 구조입니다.
- 아직 채우지 않은 항목은 부제 자리에 **짧은 가로 밑줄**만 둡니다.
  빈 화면이 아니라 "채워질 자리"로 읽히게 하는 장치이고,
  수험번호 칸에도 같은 밑줄을 씁니다.

### 4. 유의사항

테두리 안에 `※` 로 시작하는 작은 글씨 세 줄:

- 시험 시간은 실제 수능 시간표를 따랐다는 안내
- 3교시·5교시가 아직 비어 있다는 안내 (해당 부분은 빨간색 강조)
- 4교시는 준비되는 대로 채운다는 안내

### 없는 것

버튼, 링크, 입력란, 메뉴가 하나도 없습니다. 읽기만 하는 페이지입니다.
움직임(애니메이션)도 없습니다.

## 색

밝은 화면과 어두운 화면 두 벌을 만들어 두었고, 보는 사람의 설정을 따라갑니다.
쓰임새마다 이름을 붙여 관리하며, 어두운 화면은 밝은 화면을 그대로 뒤집지 않고
대비가 유지되도록 따로 골랐습니다.

| 쓰임 | 밝은 화면 | 어두운 화면 |
|---|---|---|
| 책상 (바깥 배경) | `#EDEBE7` | `#0F0F11` |
| 시험지 (본문 배경) | `#FCFCFA` | `#1A1A1D` |
| 먹 (본문 글자) | `#16161A` | `#EDEBE7` |
| 흐린 글자 (라벨·부제) | `#6F6C6B` | `#9A9693` |
| 굵은 테두리 | `#CFCBC4` | `#45423E` |
| 가는 줄 구분선 | `#E4E0D9` | `#2C2A2C` |
| 음영 줄 (쉬는 시간) | `#F3F1EC` | `#222225` |
| 도장 빨강 (강조) | `#B32821` | `#E4695F` |

강조색은 **도장 빨강 하나뿐**입니다. `미정` 도장과 유의사항의 강조 문구,
두 군데에만 씁니다. 나머지는 전부 먹과 회색으로 눌러서, 빨강이 나올 때
확실히 눈에 걸리게 했습니다.

회색 계열은 순회색이 아니라 아주 옅은 노란기가 도는 종이색 쪽으로 잡았습니다.

## 글꼴

세 가지를 역할별로 나눠 씁니다. 모두 Google Fonts에서 불러오고,
못 받아왔을 때를 대비해 윈도우·맥의 기본 한글 글꼴을 대체로 지정해 두었습니다.

| 역할 | 글꼴 | 쓰는 곳 |
|---|---|---|
| 제목 (명조) | **Gowun Batang** | 큰 제목 "자기소개 시간표", "아직 없어요…" |
| 본문 (고딕) | **IBM Plex Sans KR** | 이름, 소속, 영역 이름, 유의사항 등 대부분 |
| 숫자·라벨 (고정폭) | **IBM Plex Mono** | 교시, 시간, 소요 분, 작은 라벨, 튜닝 표기 |

- 숫자는 **자릿수 폭을 고정**해서 시간 열이 세로로 딱 맞게 떨어집니다.
- 작은 라벨은 글자 사이를 넓혀 서류 느낌을 냅니다.
- 한글은 어절 단위로 줄바꿈해서 단어가 어색하게 끊기지 않습니다.

## 화면 크기

글자 크기와 여백이 화면 폭에 따라 늘고 줄어듭니다.
좁은 화면(480px 이하)에서는 표의 열 폭과 글자를 줄이고,
정보란 네 칸이 가로 나열에서 세로 쌓기로 바뀝니다.
표가 넘칠 경우 표만 좌우로 밀리고 페이지 자체는 가로로 스크롤되지 않습니다.
