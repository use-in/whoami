# DESIGN

## 무엇인가

박유진(고려대학교 인공지능학과)의 자기소개 페이지입니다.
스크롤 없이 한 화면에서 끝나는 한 장짜리 소개로, 이름·소속·취미를
**레트로 게임의 스테이지 셀렉트 화면** 형식에 얹어 보여줍니다.
아직 없는 것(관심 분야, 프로젝트, 연락처)은 감추지 않고 `???` 나
"아직 없어요…" 로 남겨, 나중에 열릴 스테이지처럼 드러냅니다.

## 화면에 있는 것

어두운 화면 위에 패널 네 덩어리가 위에서부터 쌓입니다.

### 1. 타이틀

가운데 정렬, 흰 테두리 패널입니다.

- 작은 픽셀 글씨로 `2026 · SELF INTRO` (노랑)
- 큰 제목 **자기소개** — 한글 픽셀 글꼴, 흰 글자에 빨강 그림자를 3px 어긋나게 깖
- 아래에 `STATUS SCREEN` (라벤더)
- 패널 바로 밑에 검은 받침 막대 한 줄

### 2. 상태 정보란

흐린 테두리의 네 칸이 가로로 나란히 놓입니다. 각 칸은 픽셀 라벨 + 값입니다.

| 칸 | 값 |
|---|---|
| PLAYER | 박유진 |
| GUILD | 고려대학교 인공지능학과 |
| PLAYER ID | *비어 있음 — `- - - -`* |
| STATUS | 빨간 테두리 칸에 `미정` |

### 3. 스테이지 카드

세 칸씩 놓이는 카드 다섯 장입니다. 흰 테두리에 각진 검은 그림자가
좌우 3px, 아래 7px로 붙어 살짝 떠 보입니다. 위쪽에는 그림자가 없습니다.

| 라벨 | 번호 | 제목 | 부제 |
|---|---|---|---|
| MAJOR | 01 | 인공지능 | 고려대학교 인공지능학과 |
| HOBBY | 02 | 기타 🎸 | E A D G B E |
| INTEREST | 03 | 관심 분야 | ??? |
| PROJECT | 04 | 프로젝트 | 아직 없어요… |
| CONTACT | 05 | 연락처 | ??? |

세부 규칙:

- **MAJOR 카드에만 빨간 `▶` 커서**가 제목 앞에 붙습니다. 지금 고른 칸이라는 표시이고,
  화면에서 빨강이 쓰이는 세 군데 중 하나입니다.
- 라벨은 노랑, 번호는 흐린 보라로 좌우 끝에 갈라 놓습니다.
- 부제는 카드 아래쪽에 붙습니다. 카드 높이가 달라도 부제 줄이 가로로 맞습니다.
- 아직 채우지 않은 항목은 부제 자리에 `???` 를 둡니다.
  빈 화면이 아니라 "아직 안 열린 스테이지"로 읽히게 하는 장치이고,
  PLAYER ID 칸의 `- - - -` 도 같은 역할입니다.

### 4. NOTICE

눌린 배경의 패널 안에 `※` 로 시작하는 작은 글씨 두 줄:

- 관심 분야와 연락처가 아직 비어 있다는 안내 (빨강)
- 프로젝트는 준비되는 대로 채운다는 안내 (라벤더)

### 5. 바닥글

`© 2026 PARK YUJIN` — 픽셀 글꼴, 가운데, 흐린 보라.

### 없는 것

버튼, 링크, 입력란, 메뉴가 하나도 없습니다. 읽기만 하는 페이지입니다.
스테이지 셀렉트 모양을 빌렸을 뿐 실제로 고를 수 있는 것은 없습니다.
움직임(애니메이션)도 없습니다.

## 색

게임 화면을 흉내 낸 것이라 **어두운 화면 한 벌만** 씁니다.
보는 사람의 밝은/어두운 설정을 따라가지 않습니다.

| 쓰임 | 값 |
|---|---|
| 화면 바깥 (배경) | `#0A0A12` |
| 패널 안 | `#16163A` |
| 눌린 패널 (NOTICE) | `#0F0F28` |
| 밝은 테두리 (타이틀·카드) | `#FFFFFF` |
| 흐린 테두리 (정보란·NOTICE) | `#4A4A81` |
| 본문 글자 | `#FFFFFF` |
| 라벤더 (부제·정보 라벨) | `#7C7CB4` |
| 노랑 (스테이지 라벨) | `#FBD800` |
| 빨강 (강조) | `#D82800` |
| 연빨강 (강조 본문) | `#E3695E` |
| 그림자 | `#000000` |

노랑과 빨강은 자리를 나눠 씁니다.

- **노랑**은 스테이지 카드 라벨과 `NOTICE` 에만 씁니다. 골라서 볼 것들입니다.
- **빨강**은 세 군데뿐입니다 — 제목 그림자, `STATUS` 칸, `▶` 커서.
- 그냥 정보인 칸(PLAYER·GUILD·PLAYER ID)의 라벨은 라벤더로 눌러 둡니다.

파랑 계열은 순회색이 아니라 남색기가 도는 쪽으로 잡아, 검은 그림자와
배경이 서로 구분되게 했습니다.

## 글꼴

두 가지를 역할별로 나눠 씁니다.

| 역할 | 글꼴 | 쓰는 곳 |
|---|---|---|
| 라틴·숫자 (픽셀) | **Press Start 2P** | `2026 · SELF INTRO`, `STATUS SCREEN`, 라벨, 번호, 바닥글 |
| 한글 (픽셀) | **Galmuri11** | 제목 "자기소개", 이름, 소속, 스테이지 제목·부제, 안내문 |

- Press Start 2P는 Google Fonts에서, Galmuri11은 jsDelivr에서 불러옵니다.
- Galmuri11을 못 받아왔을 때를 대비해 윈도우의 **굴림체**를 대체로 지정했습니다.
  둘 다 픽셀 글꼴이라 못 받아와도 결이 크게 어긋나지 않습니다.
- Press Start 2P에는 한글이 없어서, 라틴 전용 자리에만 씁니다.
- 작은 라벨은 글자 사이를 넓혀 게임 UI 느낌을 냅니다.

## 화면 크기

글자 크기와 여백이 화면 폭에 따라 늘고 줄어듭니다.

- 기본은 스테이지 3열, 정보란 4열입니다.
- 640px 이하에서 둘 다 2열로 접힙니다.
- 384px 이하에서 1열로 쌓이고, 카드의 최소 높이를 풀어 내용만큼만 차지합니다.

페이지가 가로로 스크롤되지 않습니다.
